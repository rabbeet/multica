Атомарный swap rbtd → новый multirouter после lift gate.

2. **Atomic swap**: default `нет` (отдельный URL рядом без замены). Альтернативы: да, +Migration C. Если хочется replace `rbtd` — добавлю Migration C.

Дальше детали в плане.
